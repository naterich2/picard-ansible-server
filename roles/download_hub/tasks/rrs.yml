---
- name: Create Lidarr/Radarr/Sonarr/Jackett Directories
  file:
    path: "{{ item }}"
    state: directory
  with_items:
    - "{{ download_hub }}/lidarr"
    - "{{ download_hub }}/radarr"
    - "{{ download_hub }}/sonarr"
    - "{{ jackett }}"
    - "{{ jackett_torrent_blackhole }}"
  become: yes
